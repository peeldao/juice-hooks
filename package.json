{
  "name": "juice-hooks",
  "version": "1.0.0",
  "description": "Wagmi hooks for Juicebox",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-addresses": "ts-node ./scripts/generateAddresses.ts",
    "pregenerate": "npm run generate-addresses",
    "generate": "wagmi generate"
  },
  "author": "tomquirk",
  "license": "ISC",
  "devDependencies": {
    "@jbx-protocol/juice-contracts-v3": "^5.0.0",
    "@wagmi/cli": "^1.3.0",
    "dotenv": "^16.3.1"
  },
  "peerDependencies": {
    "wagmi": "^1.3.10"
  }
}
